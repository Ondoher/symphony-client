require('./src');

var runner = require('./src/users/runner');
/*
runner.start('131')
	.then(function(user) {
		user.getStreams()
			.then(function(response)
			{
				console.log('=============================================================');
				console.log(response[0]);
			});
	}).done();
*/
